# Core dependencies for training with local ultralytics source in this repository.
# Do not add "ultralytics" from PyPI here because this repo already contains `ultralytics/`.
# For GPU training on AutoDL, ensure torch/torchvision match your CUDA runtime.

numpy>=1.23.0,<3.0
matplotlib>=3.3.0,<4.0
opencv-python>=4.6.0,<5.0
Pillow>=7.1.2
PyYAML>=5.3.1
requests>=2.23.0
scipy>=1.4.1,<2.0
# torch>=1.8.0
# torchvision>=0.9.0
psutil>=5.8.0
polars>=0.20.0,<2.0

# Optional dependencies (install only if you need the related features):
# ultralytics-thop>=2.0.18  # FLOPs/profile utilities
# wakepy>=0.8.0             # optional in uestc4006p/scripts/train.py
# albumentations>=1.4.6     # extra augmentations, not required by default pipeline
# fiftyone>=0.24.0          # tools/visualize_yolo_labels_fiftyone.py
